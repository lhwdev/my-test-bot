import { command } from '../command'
import CommandParameter from '../command-parameter'



export default command({
  name: 'youtube',
  items: {}, // TODO

  async handle(p) {
    switch(p.name) {}
  }

})
